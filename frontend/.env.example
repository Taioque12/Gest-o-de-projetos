# Supabase
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxx

# Backend de leitura .mpp/.mpx (opcional — ver backend-mpp/README.md)
# Sem essa var, o import continua funcionando normalmente via XML.
VITE_MPP_API_URL=

# A chave do Gemini NÃO entra aqui — fica como secret da Edge Function
# 'analisar-ia' (supabase secrets set GEMINI_API_KEY=... --project-ref ...).
# Ver README.md, seção "Análise de IA — Edge Function".
